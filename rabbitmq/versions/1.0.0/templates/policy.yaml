kind: policy
name: {{ .Release.Name }}
tags: {}
origin: default
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.global.cpln.gvc }}/identity/{{ .Release.Name }}
targetKind: secret
targetLinks:
  - //secret/{{ .Release.Name }}-config