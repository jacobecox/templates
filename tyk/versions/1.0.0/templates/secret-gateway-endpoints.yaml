kind: secret
name: {{ .Release.Name }}-gateway-secret
description: {{ .Release.Name }} Tyk API Gateway secret
tags: {{- include "tyk.tags" . | nindent 4 }}
type: dictionary
data:
  secret: {{ .Values.gatewaySecret }}