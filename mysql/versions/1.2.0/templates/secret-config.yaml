kind: secret
name: {{ .Release.Name }}-mysql-config
description: MySQL configuration
tags:
  {{- include "mysql.tags" . | nindent 4 }}
type: dictionary
data:
  database: {{ .Values.config.db }}
  rootPassword: {{ .Values.config.rootPassword }}
  user: {{ .Values.config.user }}
  password: {{ .Values.config.password }}
{{- if .Values.backup.aws.enabled }}
  backup-bucket: {{ .Values.backup.aws.s3.bucket }}
  aws-region: {{ .Values.backup.aws.s3.region }}
{{- end }}
{{- if .Values.backup.gcp.enabled }}
  backup-bucket: {{ .Values.backup.gcp.gcs.bucket }}
{{- end }}