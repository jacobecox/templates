kind: identity
name: {{ .Release.Name }}-manticore-identity
description: Manticore identity
tags: {{- include "manticore.tags" . | nindent 4 }}
aws:
  cloudAccountLink: //cloudaccount/{{ .Values.buckets.cloudAccountName }}
  policyRefs:
    - aws::ReadOnlyAccess
    - cpln-connector
    - {{ .Values.buckets.policyName }}
