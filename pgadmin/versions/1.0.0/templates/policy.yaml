---
kind: policy
name: {{ .Release.Name }}-pgadmin-policy
description: {{ .Release.Name }}-pgadmin-policy
tags: {}
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.global.cpln.gvc }}/identity/pgadmin-identity
targetKind: secret
targetLinks:
  - //secret/pgadmin-secret
