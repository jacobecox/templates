kind: policy
name: {{ include "postgres.policyName" . }}
description: Postgres policy
tags: {{- include "pg.tags" . | nindent 4 }}
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.global.cpln.gvc }}/identity/{{ include "postgres.identityName" . }}
targetKind: secret
targetLinks:
  - //secret/{{ include "postgres.secretDatabaseName" . }}