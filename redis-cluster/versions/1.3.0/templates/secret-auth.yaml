
{{- if .Values.redis.password }}
kind: secret
name: {{ include "redis-cluster.secretAuthPasswordName" . }}
description: Redis Cluster Auth Password
tags: {{- include "redis-cluster.tags" . | nindent 4 }}
type: dictionary
data:
  REDIS_PASSWORD: {{ .Values.redis.password }}
{{- end }}