kind: policy
name: {{ include "ess.policyName" . }}
description: ESS policy
bindings:
  - permissions:
      - manage
    principalLinks:
      - //gvc/{{ .Values.global.cpln.gvc }}/identity/{{ include "ess.identityName" . }}
target: all
targetKind: secret
