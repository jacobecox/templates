---
kind: policy
name: {{ .Release.Name }}-dbeaver-policy
description: dbeaver-policy
tags: {}
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.global.cpln.gvc }}/identity/{{ .Release.Name }}-dbeaver-identity
targetKind: secret
targetLinks:
  - //secret/{{ .Release.Name }}-dbeaver-admin-config