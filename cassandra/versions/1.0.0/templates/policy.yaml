---
kind: policy
name: {{ .Release.Name }}-cassandra-{{ .Values.gvc.name }}-policy
description: {{ .Release.Name }}-cassandra-{{ .Values.gvc.name }}-policy
tags: {}
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.gvc.name }}/identity/{{ .Release.Name }}-cassandra-identity
targetKind: secret
targetLinks:
  - //secret/{{ .Release.Name }}-cassandra-startup
