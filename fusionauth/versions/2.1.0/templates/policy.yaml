kind: policy
name: {{ .Release.Name }}-auth-policy
tags:
  {{- include "fusionauth.tags" . | nindent 4 }}
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.global.cpln.gvc }}/identity/{{ .Release.Name }}-auth-identity
targetKind: secret
targetLinks:
  - //secret/{{ .Release.Name }}-pg-config
  - //secret/{{ .Release.Name }}-auth-startup