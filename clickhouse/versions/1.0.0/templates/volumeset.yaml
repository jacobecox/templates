kind: volumeset
name: {{ .Release.Name }}-vs
description: {{ .Release.Name }}-vs
tags:
  workload: {{ .Release.Name }}
spec:
  fileSystemType: ext4
  initialCapacity: 10
  performanceClass: general-purpose-ssd
  snapshots:
    createFinalSnapshot: true
    retentionDuration: 7d