kind: workload
name: {{ include "wp.name" . }}-mysql
gvc: {{ .Values.cpln.gvc }}
tags:
  {{- include "wp.tags" . | nindent 2 }}
spec:
  type: stateful
  identityLink: //identity/{{ include "wp.name" . }}
  containers:
    - name: wp
      env:
        - name: MYSQL_DATABASE
          value: 'cpln://secret/{{ include "wp.secretName" . }}.MYSQL_DATABASE'
        - name: MYSQL_ROOT_PASSWORD
          value: 'cpln://secret/{{ include "wp.secretName" . }}.MYSQL_ROOT_PASSWORD'
        - name: MYSQL_PASSWORD
          value: 'cpln://secret/{{ include "wp.secretName" . }}.MYSQL_PASSWORD'
        - name: MYSQL_USER
          value: 'cpln://secret/{{ include "wp.secretName" . }}.MYSQL_USER'
      image: 'mysql:9'
      inheritEnv: false
      cpu: {{ .Values.dbResources.cpu }}
      memory: {{ .Values.dbResources.memory }}
      ports:
        - number: 3306
          protocol: tcp
      readinessProbe:
        exec:
          command:
            - sh
            - '-c'
            - 'mysqladmin ping -u root -p${MYSQL_ROOT_PASSWORD}'
        failureThreshold: 10
        initialDelaySeconds: 17
        periodSeconds: 5
        successThreshold: 1
        timeoutSeconds: 3
      livenessProbe:
        exec:
          command:
            - sh
            - '-c'
            - 'mysqladmin ping -u root -p${MYSQL_ROOT_PASSWORD}'
        failureThreshold: 10
        initialDelaySeconds: 17
        periodSeconds: 5
        successThreshold: 1
        timeoutSeconds: 3
      volumes:
        - path: /var/lib/mysql
          recoveryPolicy: retain
          uri: 'cpln://volumeset/{{ include "wp.name" . }}'
  defaultOptions:
    autoscaling:
      metric: disabled
      minScale: 1
      maxScale: 1
    capacityAI: false
    timeoutSeconds: {{ .Values.timeoutSeconds | int }}
  firewallConfig:
    external:
      outboundAllowCIDR:
        - 0.0.0.0/0
    internal:
      inboundAllowType: same-gvc