---
kind: policy
name: cockroach-{{ .Values.gvc.name }}-policy
description: cockroach-{{ .Values.gvc.name }}-policy
tags: {}
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.gvc.name }}/identity/cockroach-identity
targetKind: secret
targetLinks:
  - //secret/cockroach-startup
  - //secret/cockroach-user