kind: policy
name: {{ include "minio.policyName" . }}
description: MinIO policy
tags:
  {{- include "minio.tags" . | nindent 4 }}
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.global.cpln.gvc }}/identity/{{ include "minio.identityName" . }}
targetKind: secret
targetLinks:
  - //secret/{{ include "minio.secretStartupName" . }}
  - //secret/{{ include "minio.secretAdminName" . }}