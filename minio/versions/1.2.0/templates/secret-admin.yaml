kind: secret
name: {{ include "minio.secretAdminName" . }}
description: MinIO admin credentials
tags: {{- include "minio.tags" . | nindent 4 }}
type: dictionary
data:
  username: {{ .Values.admin.username }}
  password: {{ .Values.admin.password }}