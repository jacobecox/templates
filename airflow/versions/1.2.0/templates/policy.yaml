# Helm template for Airflow policies
---
kind: policy
name: {{ include "airflow.policyName" . }}
description: Airflow policy
tags:
  {{- include "airflow.tags" . | nindent 4 }}
bindings:
  - permissions:
      - reveal
    principalLinks:
      - //gvc/{{ .Values.gvc.name }}/identity/{{ include "airflow.identityName" . }}
targetKind: secret
targetLinks:
  - //secret/{{ include "airflow.secretName" . }}