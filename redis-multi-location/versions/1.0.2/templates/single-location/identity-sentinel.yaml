{{- if .Values.locations }}
{{- else }}
kind: identity
name: {{ include "redis.sentinelIdentityName" . }}
description: Sentinel Identity
tags: {{- include "redis.tags" . | nindent 4 }}
gvc: {{ .Values.global.cpln.gvc }}
{{- end }}